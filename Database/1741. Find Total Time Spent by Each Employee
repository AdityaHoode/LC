select 
event_day as day, emp_id,
sum(out_time-in_time) as total_time
from 
employees
group by event_day, emp_id

-- R1
select
event_day as day,
emp_id,
sum(dur) as total_time
from (
    select
    *,
    out_time-in_time as dur
    from employees
) a
group by 1,2